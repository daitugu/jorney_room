<div class="container">
   <div class="col-lg-4 ">
     <div>
        <h3><%= @user.name %></h3>
        <%= image_tag @user.get_profile_image %>
        </div>
        <h5><strong>マイページ</strong> </h5>
        <h5>登録情報</h5>
        <h4>ニックネーム</h4> <%= @user.name %>
        <div class="col-lg-0">
          <%= link_to "投稿一覧",posts_path(user_id: @user.id),class: "btn btn-success" %>
      </div>
    </div>
  </div>
</div>