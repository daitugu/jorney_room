<div class="container">



   <div class="col-lg-4 ">
 <div>
  <h3><%= @user.name %></h3>
  <%= image_tag @user.get_profile_image %>
  <% if @user == current_user %>
  <p><%= link_to "プロフィール編集", edit_user_path(current_user) %></p>
   <% end %>
</div>

  <h5><strong>マイページ</strong> </h5>
  <h5>登録情報</h5>
<%= form_with model: @user  do |f| %>

  <h4>ニックネーム</h4> <%= f.text_field :name, readonly: true  %>
  <h4>メールアドレス</h4>  <%= f.text_field :email, readonly: true %><br>
 <% end %>
<div class="col-lg-0">
<%= link_to "投稿一覧",posts_path(user_id: @user.id),class: "btn btn-success" %>




    <%= link_to "#{@user.name}のブックマーク", bookmarks_index_bookmarks_path(id: @user.id) %>

<%= link_to "編集する" ,edit_user_path(current_user) , class: "btn btn-success"%>
<%= link_to  "下書き" ,draft_path , class: "btn btn-success"%>
</div>
</div>
</div>

</div>